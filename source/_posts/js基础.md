 ---
title: base
---

#### 字符串截取
> slice(start, end)
1. 支持负数, -1即最后一位 slice(0, -1) 截掉最后一位
> substring(start, end) 
1. 不支持负数
2. 两个参数中较小一个作为起始位置  substring(2, 4) = substring(4, 2)
> substr(start, len) 


